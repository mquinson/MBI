#! /bin/sh

# Go to the MBI root directory
cd "$(dirname "$0")"/../
rootdir=$(pwd)

set -e # Fail on error

# Run the script on each code
python3 ${rootdir}/MBI.py -x parcoach $(ls ${rootdir}/codes/*.c)
