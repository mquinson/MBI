#! /bin/sh

set -e # Fail on error

# Go to the MBI scripts/ directory
cd "$(dirname "$0")"

./CIVL-build
./CIVL-run
./ISP-build
./ISP-run
./MUST-build
./MUST-run
./Parcoach-build
./Parcoach-run
./SimGrid-build
./SimGrid-run